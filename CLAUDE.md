# ExpenseFlow - Claude Code向け開発指示書

## プロジェクト概要（Claude Codeへの共有情報）

ExpenseFlowは、中小企業向けの「経費精算＋入金確認」を一元化した業務効率化ツールのMVPです。  
特に「支出承認時に対応する入金予定の登録を意識させるガバナンス機能」と「CSVアップロード＋AI自動マッチング（下4桁マスキング含む）」を特徴としています。

開発は1週間集中型で行い、**「動くもの」を最優先**に、将来的な拡張性を意識した設計を心がけます。

## 技術スタック（固定）

- **フロントエンド**：Next.js 15 (App Router) + TypeScript
- **スタイリング**：Tailwind CSS + shadcn/ui
- **バックエンド**：Supabase（認証・DB・Storage・RLS）
- **AI**：
  - 領収書解析：Claude Vision（Anthropic API）
  - 入金マッチング：カスタムロジック＋将来的にLLM活用を検討
- **その他**：Zod、Recharts（簡易ダッシュボード用）

## 開発の基本方針（必ず守ってください）

### 1. 柔軟性と拡張性を最優先
- 具体的な数値（px, rem, 画面幅など）は一切ハードコードしない
- 将来的に機能追加・デザイン変更がしやすい構造を意識
- 「今はシンプルに、将来的に拡張しやすい形」を常に念頭に置く

### 2. セキュリティ意識を常に持つ
- 口座番号は下4桁のみマスキング保存（フル口座番号は登録不可）
- Supabase RLSを徹底的に活用し、組織（会社）単位でデータが完全に隔離される設計を基本とする
- 個人情報・金融情報は必要最小限に留め、漏洩リスクを意識したデータ設計を行う

### 3. ユーザー体験のシンプルさ
- 中小企業の経理担当者が「迷わず使える」ことを最優先
- AIの提案は「信頼度」や「根拠」を明確に表示し、ユーザーが判断しやすい形にする
- エラーや警告は原因と次のアクションがすぐにわかるメッセージ設計を心がける

## 各画面・機能の実装ガイドライン

### 経費精算関連
- 領収書アップロード → Claude Vision解析 → 結果確認・修正 → 承認申請の流れをスムーズに
- 支出承認画面では「対応する入金予定」の選択・確認を自然なフローとして実装（将来的なガバナンス強化を見据えて）
- 解析結果の修正は最小限の操作で完了できるように設計

### 入金確認関連
- CSVアップロード機能は、主要な銀行形式（三菱UFJ、SMBC、みずほなど）を想定した柔軟なパース処理を推奨
- AIマッチングは「名義一致」「金額一致」「口座下4桁一致」「期日近さ」などの要素を組み合わせてスコアリング
- マッチング提案時は「なぜこの取引先と判断したか」の根拠を明確に表示（ユーザーの確認を助ける）
- 振込名義エイリアス登録は、1つの取引先に対して複数登録できるシンプルなUIを意識

### ガバナンス関連（差別化ポイント）
- 支出承認時に「対応する入金予定」の有無を視覚的に確認できるようにする
- 入金予定が未登録の場合の警告・誘導フローを自然に組み込む（将来的に「未登録では承認不可」ルール化も視野）
- この部分は「ただのツール」ではなく「業務ルールを支援するツール」であることを体現する重要なポイント

## セキュリティ実装の指針

- SupabaseのRow Level Security（RLS）を全テーブルに適用し、「自社のデータしか見えない」状態を徹底
- 環境変数は`.env.local`で管理し、Gitに絶対に上げない
- 入力値はZodで厳密にバリデーションし、XSSや不正データ挿入を防止
- 将来的な本番運用を見据え、「暗号化保存」「アクセスログ」「定期的な脆弱性診断」の追加をTODOとして残す

## レスポンシブ・レイアウトの考え方

- 具体的なブレークポイントは固定せず、コンテンツの優先度に応じて柔軟にレイアウトが変化する形を推奨
- PCでは横並びを活かした情報密度の高い表示、スマホでは縦積みで操作性を重視
- すべての画面で「PCでもスマホでも自然に使える」状態を目指す

## Claude Codeへのお願い

- コードは**読みやすく、コメントを適切に残す**形で書いてください
- 将来的に他のエンジニア（または未来の自分）が読んでも理解できるように、背景や意図を軽くコメントに残す
- 「この部分は将来的にXXを追加する可能性があるので、柔軟に拡張できる形にしておく」といったTODOコメントも積極的に残してください
- セキュリティやパフォーマンスで気になる点があれば、遠慮なく指摘・提案してください

## 連絡・確認事項

- 実装中に判断に迷う点があれば、遠慮なく質問してください
- 「この機能はMVPではシンプルに、将来的に拡張する形にしておく」といった判断も歓迎します
- 毎日小さな進捗を共有しながら、1週間で「動くMVP」を完成させることを目指します

---

**ExpenseFlowを一緒に良いものにしていきましょう。**

*この指示書はMVP開発開始時点のものです。開発中に方針変更があった場合は随時更新します。*
