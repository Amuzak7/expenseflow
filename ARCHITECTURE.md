# ExpenseFlow アーキテクチャ概要

## 導入

ExpenseFlowは、中小企業が「支出の承認」と「入金の予定」を連動させて管理できるように設計された業務効率化ツールのMVPです。  
単なる経費精算ツールではなく、**キャッシュフローのガバナンスを支援する**という思想のもと、技術選定とシステム構成を行っています。

このドキュメントでは、システム全体の構成・技術選定の背景・データフロー・セキュリティ設計の考え方を高レベルで整理します。

## 全体アーキテクチャの思想

### 基本方針
- **シンプルさと拡張性の両立**を最優先に設計
- 1週間MVPで「動くもの」を作りつつ、将来的な機能追加（銀行API連携、AIの高度化など）がしやすい構造を意識
- セキュリティと実用性のバランスを重視し、過度に複雑な構成は避ける

### レイヤー構成の考え方
- **フロントエンド層**：ユーザーインターフェースとビジネスロジックの軽い処理を担当
- **バックエンド層**：データ永続化、認証、認可、セキュリティ境界を担当（Supabaseに集約）
- **AI層**：領収書解析と入金マッチングの知能部分を担当（外部API＋カスタムロジック）
- **データ層**：組織（会社）単位でのデータ隔離を徹底

この構成により、将来的にAI部分を置き換えたり、バックエンドを拡張したりする際の影響を最小限に抑えられるように設計しています。

## 技術選定の背景と理由

### Next.js 15 (App Router) + TypeScript
- フルスタック開発がしやすく、サーバーサイドレンダリングとAPIルートを柔軟に扱える
- TypeScriptによる型安全性で、長期的なメンテナンス性を高める
- Vercelとの親和性が高く、ポートフォリオ公開も容易

### Supabase
- 認証・データベース・ストレージ・RLS（Row Level Security）を一元的に提供
- 組織（会社）単位でのデータ完全隔離が標準機能で実現できる点が、セキュリティ要件に合致
- 開発速度を重視したMVPにおいて、バックエンドのインフラ構築コストを大幅に削減

### Claude Vision（Anthropic）
- 日本語の領収書・請求書に対するOCR精度と構造化抽出能力が高い
- 画像解析だけでなく、意味理解を伴ったデータ抽出が可能
- 将来的に他のLLMへ置き換えやすいAPI設計を採用

### カスタム入金マッチングロジック
- 現時点ではルールベース＋スコアリングを中心とし、将来的にLLMを組み合わせる拡張性を残す
- 口座番号は下4桁のみマスキング保存する設計により、セキュリティと実用性を両立

## データフロー概要

### 経費精算の流れ（支出側）
1. ユーザーが領収書・請求書をアップロード
2. Claude Visionが画像を解析し、構造化データ（金額、日付、取引先、税区分など）を抽出
3. ユーザーが解析結果を確認・修正
4. 支出申請として保存（Supabase）
5. 承認ワークフローを経て確定（将来的に「対応入金予定」の登録状況を考慮したガバナンスを強化予定）

### 入金確認の流れ（収入側）
1. ユーザーが銀行明細のCSVをアップロード
2. システムがCSVをパースし、取引日・金額・振込人名義などを抽出
3. AIマッチングエンジンが以下の要素を組み合わせてスコアリング
   - 取引先名義（エイリアス含む）
   - 金額の一致度
   - 口座番号下4桁の一致
   - 入金期日との近さ
4. ユーザーが提案されたマッチングを確認・修正・確定
5. 確定した入金情報で請求書のステータスを更新

### 取引先マスタの役割
- 1つの取引先に対して複数の振込名義（エイリアス）を登録可能
- 口座情報は下4桁のみマスキング保存
- 将来的に銀行API連携時に、口座情報との照合精度を高めるための基盤として機能

## セキュリティアーキテクチャ

### 基本原則
- **データ最小化**：必要最小限の情報のみを保存（口座番号は下4桁のみ）
- **アクセス制御の徹底**：Supabase RLSにより、ログインした組織のデータしか一切見えない状態を保証
- **漏洩リスクの低減**：フル口座番号を保存しない設計により、万一の漏洩時でも影響を限定

### 将来的なセキュリティ強化ポイント（TODO）
- 振込名義・口座下4桁などの機微情報の暗号化保存
- アクセスログの記録と監査機能
- 定期的な脆弱性診断とペネトレーションテスト
- 本番環境でのWAF導入やSOC2相当のコンプライアンス対応

## 拡張性と将来の考慮点

### 想定される拡張シナリオ
- **銀行API連携**：現在はCSVアップロードで代替しているが、将来的に公式銀行APIやfreee/MF APIとの連携を想定したインターフェース設計
- **AIの高度化**：入金マッチングの精度向上や、督促文面の自動生成など、LLMのさらなる活用
- **ガバナンスの強化**：支出承認時に「入金予定未登録では承認不可」といったルールベースの制御をシステムレベルで実装
- **モバイル対応**：PWAやネイティブアプリへの展開を視野に、API設計を意識

### 設計上の制約とトレードオフ
- 1週間MVPのため、完全な銀行API連携は見送り（CSV＋モックで代替）
- セキュリティは「現時点で十分な水準」を目指しつつ、本番運用時の強化を明確にTODOとして残す
- 機能の豊富さよりも「中小企業がすぐに使い始められるシンプルさ」を優先

## まとめ

ExpenseFlowのアーキテクチャは、以下の3つの軸で設計されています：

1. **実用性**：CSVアップロードとAIマッチングにより、すぐに業務で価値を発揮できる
2. **セキュリティ**：口座情報のマスキングとRLSによる組織間隔離で、企業が安心して利用できる基盤を提供
3. **拡張性**：将来的な銀行API連携やAIの高度化、ガバナンス機能の強化を見据えた柔軟な構成

この設計思想を維持しながら、MVP開発を進めていきます。

---

*このドキュメントはMVP開発開始時点のスナップショットです。開発中にアーキテクチャの変更があった場合は随時更新してください。*
